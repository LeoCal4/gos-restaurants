[metadata]
license_file = LICENSE

[bdist_wheel]
universal = True

[pytest]
testpaths =
    tests
log_cli = True
addopts =
    ; --cov=restaurants
    -r a
    -v

[coverage:run]
source =
    gos-restaurants
